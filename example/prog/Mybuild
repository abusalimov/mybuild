
package('myprog')

module (
	'init1',

	#implements = 
	sources = ['init1.c'],
)

module (
	'init2',

	#implements = 
	sources = ['init2.c'],
)

module (
	'body',

	sources = ['body.c'],
)

module (
	'prog',

	depends = ['init1', 'body'],
)
